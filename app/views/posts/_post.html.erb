	<tr>
		<td><%= post.id %></td>
		<td><%= post.title %></td>
		<% if can_destroy_post?(post) %>
		<td><%= link_to "Destroy", post_path(post.id), class: "button", method: :delete %></td>
		<% end %>
	</tr>